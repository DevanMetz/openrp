services:
  openrp:
    build: .
    ports:
      - '3000:3000'
    environment:
      SERVER_NAME: 'OpenRP | Union District'
      STARTING_MONEY: '1500'
    volumes:
      - openrp-data:/app/data
    restart: unless-stopped
volumes:
  openrp-data:
