services:
  openrp:
    build: .
    ports:
      - '3000:3000'
    environment:
      SERVER_NAME: 'OpenRP | Union District'
      MAX_PLAYERS: '32'
      STARTING_MONEY: '1500'
    volumes:
      - openrp-data:/app/data
    restart: unless-stopped
volumes:
  openrp-data:
